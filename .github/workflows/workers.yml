name: Deploy — Workers

# Builds the workers Docker image (Cloud Tasks handlers: image / video / merge)
# and deploys to Cloud Run. Workers are NOT publicly accessible — invoked via
# Cloud Tasks using OIDC.
#
# Prod deploys automatically on merge to main (single active environment).
#
# TODO: When a separate staging environment is stood up, reinstate the
# two-stage flow: auto-deploy staging first, then require manual approval
# for prod (mirrors api.yml pattern).
#
# GCP authentication (WIF or SA key via GCP_CREDENTIALS) is provisioned and
# owned outside of this repo's Terraform.
#
# The workers image reuses shared service modules (AI providers, Firestore/
# FCM, GCS, config, instructions) owned by backend/app rather than
# duplicating them — see workers/Dockerfile. Build context is the repo root,
# not workers/, so the Dockerfile can COPY from backend/. The path trigger
# below covers both workers/** and the specific backend/ paths workers
# depends on, so a shared-module change rebuilds workers too.

on:
  push:
    branches:
      - main
    paths:
      - 'workers/**'
      - 'backend/app/services/**'
      - 'backend/app/config.py'
      - 'backend/instructions/**'
      - 'backend/pyproject.toml'
  workflow_dispatch:

permissions:
  contents: read
  id-token: write  # Required for Workload Identity Federation auth to GCP

env:
  IMAGE_BASE: ${{ vars.GCP_REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/arkmask/workers

jobs:
  build:
    name: Build & Push Workers Image
    runs-on: ubuntu-latest
    outputs:
      image: ${{ steps.meta.outputs.image }}

    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ vars.GCP_WIF_PROVIDER }}
          service_account: ${{ vars.GCP_SA }}

      - name: Setup gcloud CLI
        uses: google-github-actions/setup-gcloud@v3

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ vars.GCP_REGION }}-docker.pkg.dev --quiet

      - name: Build and push workers image
        id: meta
        run: |
          SHORT_SHA="${GITHUB_SHA:0:7}"
          IMAGE="${{ env.IMAGE_BASE }}:${SHORT_SHA}"
          docker build -t "${IMAGE}" -t "${{ env.IMAGE_BASE }}:latest" \
            -f workers/Dockerfile .
          docker push "${IMAGE}"
          docker push "${{ env.IMAGE_BASE }}:latest"
          echo "image=${IMAGE}" >> $GITHUB_OUTPUT


  deploy-prod:
    name: Deploy Workers to Prod
    runs-on: ubuntu-latest
    needs: build
    if: needs.build.result == 'success'
    environment: prod

    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ vars.GCP_WIF_PROVIDER }}
          service_account: ${{ vars.GCP_SA }}

      - name: Setup gcloud CLI
        uses: google-github-actions/setup-gcloud@v3

      - name: Deploy workers to Cloud Run (prod)
        run: |
          gcloud run deploy prod-arkmask-workers \
            --image="${{ needs.build.outputs.image }}" \
            --region="${{ vars.GCP_REGION }}" \
            --project="${{ vars.GCP_PROJECT_ID }}" \
            --quiet
