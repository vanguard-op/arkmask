// Placeholder widget test — feature-level tests live in test/features/.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder test passes', (tester) async {
    expect(true, isTrue);
  });
}
